added course 
<% if activity.trackable %>
  <%= link_to activity.trackable.name, user_course_path(activity.trackable.user_id,activity.trackable) %>
<% else %>
  which has since been removed 
<% end %>